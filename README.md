# Piyush Birthday Website ❤️

Romantic birthday website for Piyush.

## Production verification

GitHub Actions runs the following checks on every push to `main`:

```bash
npm install
npm run typecheck
npm run lint
npm run build
```

The repository is ready for local development with `npm run dev`.

<!-- CI trigger: production verification -->
